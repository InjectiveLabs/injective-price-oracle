// Code generated by goa v3.7.0, DO NOT EDIT.
//
// PriceOracle HTTP client CLI support package
//
// Command:
// $ goa gen github.com/InjectiveLabs/injective-price-oracle/api/design -o ../

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	healthc "github.com/InjectiveLabs/injective-price-oracle/api/gen/http/health/client"
	injectivepriceoracleapic "github.com/InjectiveLabs/injective-price-oracle/api/gen/http/injective_price_oracle_api/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() string {
	return `injective-price-oracle-api probe
health get-status
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` injective-price-oracle-api probe --body '{
      "content": "Q29uc2VxdWF0dXIgZWEgdm9sdXB0YXRlbSBpbi4="
   }'` + "\n" +
		os.Args[0] + ` health get-status` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
	injectivePriceOracleAPIProbeEncoderFn injectivepriceoracleapic.InjectivePriceOracleAPIProbeEncoderFunc,
) (goa.Endpoint, interface{}, error) {
	var (
		injectivePriceOracleAPIFlags = flag.NewFlagSet("injective-price-oracle-api", flag.ContinueOnError)

		injectivePriceOracleAPIProbeFlags    = flag.NewFlagSet("probe", flag.ExitOnError)
		injectivePriceOracleAPIProbeBodyFlag = injectivePriceOracleAPIProbeFlags.String("body", "REQUIRED", "")

		healthFlags = flag.NewFlagSet("health", flag.ContinueOnError)

		healthGetStatusFlags = flag.NewFlagSet("get-status", flag.ExitOnError)
	)
	injectivePriceOracleAPIFlags.Usage = injectivePriceOracleAPIUsage
	injectivePriceOracleAPIProbeFlags.Usage = injectivePriceOracleAPIProbeUsage

	healthFlags.Usage = healthUsage
	healthGetStatusFlags.Usage = healthGetStatusUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "injective-price-oracle-api":
			svcf = injectivePriceOracleAPIFlags
		case "health":
			svcf = healthFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "injective-price-oracle-api":
			switch epn {
			case "probe":
				epf = injectivePriceOracleAPIProbeFlags

			}

		case "health":
			switch epn {
			case "get-status":
				epf = healthGetStatusFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "injective-price-oracle-api":
			c := injectivepriceoracleapic.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "probe":
				endpoint = c.Probe(injectivePriceOracleAPIProbeEncoderFn)
				data, err = injectivepriceoracleapic.BuildProbePayload(*injectivePriceOracleAPIProbeBodyFlag)
			}
		case "health":
			c := healthc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "get-status":
				endpoint = c.GetStatus()
				data = nil
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// injective-price-oracle-apiUsage displays the usage of the
// injective-price-oracle-api command and its subcommands.
func injectivePriceOracleAPIUsage() {
	fmt.Fprintf(os.Stderr, `Injective-Price-Oracle services API doc
Usage:
    %[1]s [globalflags] injective-price-oracle-api COMMAND [flags]

COMMAND:
    probe: Validate TOML file

Additional help:
    %[1]s injective-price-oracle-api COMMAND --help
`, os.Args[0])
}
func injectivePriceOracleAPIProbeUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] injective-price-oracle-api probe -body JSON

Validate TOML file
    -body JSON: 

Example:
    %[1]s injective-price-oracle-api probe --body '{
      "content": "Q29uc2VxdWF0dXIgZWEgdm9sdXB0YXRlbSBpbi4="
   }'
`, os.Args[0])
}

// healthUsage displays the usage of the health command and its subcommands.
func healthUsage() {
	fmt.Fprintf(os.Stderr, `HealthAPI allows to check if backend data is up-to-date and reliable or not.
Usage:
    %[1]s [globalflags] health COMMAND [flags]

COMMAND:
    get-status: Get current backend health status

Additional help:
    %[1]s health COMMAND --help
`, os.Args[0])
}
func healthGetStatusUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] health get-status

Get current backend health status

Example:
    %[1]s health get-status
`, os.Args[0])
}
